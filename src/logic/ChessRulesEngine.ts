import { Piece, Move } from "./types";

/**
 * Validates a move on the board.
 * For Stage 1, this function accepts all moves.
 */
export const isValidMove = (board: (Piece | null)[][], move: Move): boolean => {
  // Placeholder logic: accept all moves.
  return true;
};
